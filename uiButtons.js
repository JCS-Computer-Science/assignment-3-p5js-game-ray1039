class Button {
    constructor(sizeX, sizeY, addPos, isSelected, text, textX, g, id) {
        this.sizeX = sizeX;
        this.sizeY = sizeY;
        this.addPos = addPos;
        this.isSelected = isSelected;
        this.text = text;
        this.textX = textX;
        this.g = g;
        this.id = id;
    }
}